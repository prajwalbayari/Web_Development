<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head') %>
    <title>Blogify - Home</title>
    <style>
      .hero-section {
        background: linear-gradient(135deg, #374151 0%, #6b7280 50%, #9ca3af 100%);
        color: white;
        padding: 5rem 0;
      }
      .blog-card {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        border: none;
        border-radius: 20px;
        overflow: hidden;
        box-shadow: 0 8px 25px rgba(55, 65, 81, 0.12);
        background: white;
      }
      .blog-card:hover {
        transform: translateY(-8px);
        box-shadow: 0 20px 40px rgba(55, 65, 81, 0.2);
      }
      .card-img-top {
        height: 240px;
        object-fit: cover;
        transition: transform 0.4s ease;
      }
      .blog-card:hover .card-img-top {
        transform: scale(1.05);
      }
      .blog-title {
        font-size: 1.25rem;
        font-weight: 700;
        color: #374151;
        margin-bottom: 0.75rem;
        line-height: 1.4;
      }
      .blog-excerpt {
        color: #6b7280;
        font-size: 0.95rem;
        line-height: 1.6;
      }
      .author-info {
        font-size: 0.85rem;
        color: #9ca3af;
      }
      .btn-read-more {
        background: linear-gradient(135deg, #374151 0%, #6b7280 100%);
        border: none;
        border-radius: 12px;
        padding: 10px 24px;
        font-size: 0.9rem;
        font-weight: 600;
        transition: all 0.3s ease;
        box-shadow: 0 4px 14px rgba(55, 65, 81, 0.25);
      }
      .btn-read-more:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(55, 65, 81, 0.35);
        background: linear-gradient(135deg, #4b5563 0%, #6b7280 100%);
      }
      .welcome-card {
        background: linear-gradient(135deg, #6b7280 0%, #9ca3af 100%);
        border: none;
        border-radius: 24px;
        color: white;
        box-shadow: 0 12px 35px rgba(107, 114, 128, 0.15);
      }
      .section-title {
        font-size: 2.25rem;
        font-weight: 800;
        color: #374151;
        margin-bottom: 2.5rem;
        position: relative;
      }
      .section-title::after {
        content: '';
        width: 80px;
        height: 5px;
        background: linear-gradient(135deg, #374151 0%, #6b7280 50%, #9ca3af 100%);
        position: absolute;
        bottom: -12px;
        left: 0;
        border-radius: 3px;
      }
      .empty-state {
        background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
        border-radius: 24px;
        padding: 4rem 2rem;
        text-align: center;
        border: 2px solid #e2e8f0;
      }
      .empty-state-icon {
        font-size: 4.5rem;
        color: #cbd5e1;
        margin-bottom: 1.5rem;
      }
      .hero-content h1 {
        font-size: 3.5rem;
        font-weight: 900;
        line-height: 1.1;
      }
      .hero-content .lead {
        font-size: 1.25rem;
        opacity: 0.9;
      }
      .btn-hero {
        border-radius: 16px;
        padding: 1rem 2rem;
        font-weight: 600;
        font-size: 1.1rem;
      }
    </style>
  </head>
  <body>
    <%- include('./partials/nav') %>
    
    <!-- Hero Section -->
    <% if (!locals.user) { %>
    <section class="hero-section">
      <div class="container">
        <div class="row justify-content-center text-center">
          <div class="col-lg-8">
            <div class="hero-content">
              <h1 class="display-3 fw-bold mb-4">Welcome to Blogify</h1>
              <p class="lead mb-4">Discover amazing stories, share your thoughts, and connect with a community of passionate writers from around the world.</p>
              <div class="d-flex justify-content-center gap-3 flex-wrap">
                <a href="/user/signup" class="btn btn-light btn-hero btn-lg px-4">
                  <i class="bi bi-person-plus me-2"></i>Get Started
                </a>
                <a href="/user/signin" class="btn btn-outline-light btn-hero btn-lg px-4">
                  <i class="bi bi-box-arrow-in-right me-2"></i>Sign In
                </a>
              </div>
              <p class="mt-4 text-white-50">
                <i class="bi bi-shield-check me-2"></i>
                Free to join • Unlimited reading • Community driven
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>
    <% } else { %>
    <!-- Welcome Back Section for Authenticated Users -->
    <section class="hero-section">
      <div class="container">
        <div class="row justify-content-center text-center">
          <div class="col-lg-8">
            <div class="hero-content">
              <h1 class="display-4 fw-bold mb-3">Welcome back, <%= user.fullName || user.email %>!</h1>
              <p class="lead mb-4">Ready to share your next story or discover something new?</p>
              <div class="d-flex justify-content-center gap-3 flex-wrap">
                <a href="/blog/add-new" class="btn btn-light btn-hero btn-lg px-4">
                  <i class="bi bi-plus-circle me-2"></i>Write New Blog
                </a>
                <a href="#blogs" class="btn btn-outline-light btn-hero btn-lg px-4">
                  <i class="bi bi-arrow-down-circle me-2"></i>Browse Blogs
                </a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
    <% } %>

    <!-- Main Content -->
    <div class="container my-5" id="blogs">
      <!-- Latest Blogs Section -->
      <div class="row">
        <div class="col-12">
          <h2 class="section-title">
            <% if (locals.user) { %>
              Discover Stories
            <% } else { %>
              Latest Blogs
            <% } %>
          </h2>
          
          <% if (blogs && blogs.length > 0) { %>
          <div class="row g-4">
            <% blogs.forEach(blog => { %>
            <div class="col-lg-4 col-md-6 col-sm-12">
              <div class="card blog-card h-100">
                <% if (blog.coverImageURL) { %>
                <img src="<%= blog.coverImageURL %>" class="card-img-top" alt="<%= blog.title %>">
                <% } else { %>
                <div class="card-img-top d-flex align-items-center justify-content-center bg-light">
                  <i class="bi bi-image text-muted" style="font-size: 3rem;"></i>
                </div>
                <% } %>
                
                <div class="card-body d-flex flex-column p-4">
                  <h5 class="blog-title"><%= blog.title %></h5>
                  <p class="blog-excerpt flex-grow-1">
                    <%= blog.body.length > 120 ? blog.body.substring(0, 120) + '...' : blog.body %>
                  </p>
                  
                  <div class="mt-auto">
                    <div class="author-info d-flex align-items-center justify-content-between mb-3">
                      <div class="d-flex align-items-center">
                        <i class="bi bi-person-circle me-2"></i>
                        <span><%= blog.createdBy.fullName || blog.createdBy.email %></span>
                      </div>
                      <div class="d-flex align-items-center">
                        <i class="bi bi-calendar3 me-1"></i>
                        <span><%= new Date(blog.createdAt).toLocaleDateString() %></span>
                      </div>
                    </div>
                    <% if (locals.user) { %>
                    <a href="/blog/<%= blog._id %>" class="btn btn-read-more text-white w-100">
                      <i class="bi bi-arrow-right me-2"></i>Read More
                    </a>
                    <% } else { %>
                    <a href="/user/signin" class="btn btn-read-more text-white w-100">
                      <i class="bi bi-lock me-2"></i>Sign In to Read
                    </a>
                    <% } %>
                  </div>
                </div>
              </div>
            </div>
            <% }) %>
          </div>
          <% } else { %>
          <div class="empty-state">
            <div class="empty-state-icon">
              <i class="bi bi-journal-x"></i>
            </div>
            <h4 class="fw-bold text-muted">No blogs yet</h4>
            <p class="text-muted mb-4">Be the first to share your amazing story with the world!</p>
            <% if (locals.user) { %>
            <a href="/blog/add-new" class="btn btn-primary btn-lg rounded-pill px-4">
              <i class="bi bi-plus-circle me-2"></i>Write Your First Blog
            </a>
            <% } %>
          </div>
          <% } %>
        </div>
      </div>
    </div>

    <%- include('./partials/scripts') %>
  </body>
</html>
